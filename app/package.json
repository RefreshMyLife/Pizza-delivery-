{
  "name": "json-server",
  "version": "1.0.0",
  "description": "test json-server",
  "main": "index.js",
  "json-server": "^0.17.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.0"
  }
}
